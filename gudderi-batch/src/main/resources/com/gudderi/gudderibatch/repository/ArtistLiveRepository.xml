<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gudderi.gudderibatch.repository.ArtistLiveRepository">
    <select id="selectArtistId" resultType="java.lang.Integer">
        SELECT
            artistId
        FROM
            Artist
        WHERE
            artistName = #{artistName}
    </select>


    <insert id="insertArtist" parameterType="com.gudderi.gudderibatch.domain.Artist">
        INSERT INTO Artist (
            artistName,
            createdAt
        )
        VALUES
        (
            #{artist.artistName},
            #{_now}
        )
        <selectKey resultType="java.lang.Integer" keyProperty="artistId" order="AFTER">
            SELECT artistId AS artistId FROM Artist WHERE artistName = #{artist.artistName}
        </selectKey>
    </insert>

     <select id="selectLiveId" resultType="java.lang.Integer">
                SELECT
                    liveId
                FROM
                    Live
                WHERE
                    liveName = #{liveName}
     </select>

    <insert id="insertLive" parameterType="com.gudderi.gudderibatch.domain.Live">
        INSERT INTO Live (
            liveName,
            artistId,
            createdAt
        )
        VALUES
        (
            #{live.liveName},
            #{artistId},
            #{_now}
        )
        <selectKey resultType="java.lang.Integer" keyProperty="liveId" order="AFTER">
            SELECT liveId AS liveId FROM Live WHERE liveName = #{live.liveName} AND artistId = #{artistId}
        </selectKey>
    </insert>

    <select id="existsLiveSchedule" resultType="boolean">
        SELECT EXISTS (
             SELECT
                 liveScheduleId
             FROM
                 LiveSchedule
             WHERE
                 liveId = #{liveId}
             AND liveDate = #{liveDate}
        )
    </select>

    <insert id="insertLiveSchedule" parameterType="com.gudderi.gudderibatch.domain.LiveSchedule">
            INSERT INTO LiveSchedule (
                liveId,
                livePlace,
                livePrefectureCode,
                liveDate,
                createdAt
            )
            VALUES
            (
                #{liveId},
                #{liveSchedule.livePlace},
                #{liveSchedule.livePrefecture.code},
                #{liveSchedule.liveDate},
                #{_now}
            )
    </insert>
</mapper>