<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gudderi.gudderibatch.repository.ArtistLiveRepository">
    <select id="selectArtistId" resultType="java.lang.Integer">
        SELECT
            artistId
        FROM
            Artist
        WHERE
            artistName = #{artistName}
    </select>

    <insert id="insertArtist">
        INSERT INTO Artist (
            artistName,
            createdAt
        )
        VALUES
        (
            #{artist.artistName},
            #{_now}
        )
        <selectKey resultType="int" keyProperty="artist.artistId" order="AFTER">
            SELECT LAST_INSERT_ID() AS artistId
        </selectKey>
    </insert>

     <select id="selectLiveId" resultType="java.lang.Integer">
        SELECT
            liveId
        FROM
            Live
        WHERE
            artistId = #{artistId}
        AND liveName = #{liveName}
     </select>

    <insert id="insertLive" parameterType="com.gudderi.gudderibatch.domain.Live">
        INSERT INTO Live (
            artistId,
            liveName,
            createdAt
        )
        VALUES
        (
            #{artistId},
            #{live.liveName},
            #{_now}
        )
        <selectKey resultType="int" keyProperty="live.liveId" order="AFTER">
            SELECT LAST_INSERT_ID() AS liveId
        </selectKey>
    </insert>

    <select id="existsLiveSchedule" resultType="boolean">
        SELECT EXISTS (
             SELECT
                 liveScheduleId
             FROM
                 LiveSchedule
             WHERE
                 liveId = #{liveId}
             AND liveDate = #{liveDate}
        )
    </select>

    <insert id="insertLiveSchedule" parameterType="com.gudderi.gudderibatch.domain.LiveSchedule">
            INSERT INTO LiveSchedule (
                liveId,
                livePrefectureCode,
                livePlace,
                liveDate,
                createdAt
            )
            VALUES
            (
                #{liveId},
                #{liveSchedule.livePrefecture.code},
                #{liveSchedule.livePlace},
                #{liveSchedule.liveDate},
                #{_now}
            )
    </insert>
</mapper>